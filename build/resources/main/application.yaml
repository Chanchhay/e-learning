spring:
  application:
    name: elearning-a01-m1

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/db_elearning
    driver-class-name: org.postgresql.Driver
server:
  port: 8090
